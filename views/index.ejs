<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <div>
      <% for( let index = 0; index < db.length; index++ ) { %>
      <div>
        <p><%= db[index].id  %>: <%= db[index].toDo %></p>
      </div>
      
      <% } %>
    </div>
  </body>
</html>
